<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.omarali.thecookbook">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:name=".CookBook"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:windowSoftInputMode="stateVisible"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ProfileActivity" />
        <activity android:name=".FriendsActivity" />
        <activity android:name=".CreateRecipeActivity" />
        <activity android:name=".FriendsPofileActivity" />
        <activity android:name=".TimelineActivity" />
        <activity android:name=".SettingsActivity" />
        <activity android:name=".OthersFriendsActivity" />
        <activity android:name=".NotFriendProfileActivity" />
        <activity android:name=".PrivateMessaging" />
        <activity android:name=".ViewPost" />
        <activity android:name=".private_message"></activity>
    </application>

</manifest>
